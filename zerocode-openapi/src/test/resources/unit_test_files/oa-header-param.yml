openapi: "3.0.0"
info:
  version: 1.0.0
  title: Header parameters
paths:
  /testHeaderParam:
    get:
      summary: Header parameters
      parameters:
      - in: header
        name: headerParam
        schema:
          type: string
