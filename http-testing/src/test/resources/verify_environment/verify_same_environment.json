{
    "scenarioName": "GIVEN- this 'verify_same_environment' step is to be excluded, WHEN- I invoke GET, THEN- the test step will not execute when the user passes 'Denv=ProductionTestCase' via command line",
    "steps": [
        {
            "name": "verify_same_environment",
            "url": "/users/octocat",
            "method": "GET",
            "request": {
            },
            "verify": {
                "status": 200,
                "body": {
                    "login" : "octocat",
                    "id" : 583231,
                    "type" : "User"
                }
            },
            "excludeEnvironment": "Production"
        }
    ]
}
